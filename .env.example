# Buy or Wait? — Financial Affordability Agent Environment Configuration
# The engine is fully deterministic and requires no external API keys.
# Optional overrides:
FORECAST_DAYS=90
DATASET_DIR=dataset
LOG_LEVEL=INFO
